export interface PiPlanningSummaryOptions {
  project: string;
  boardId: string;
  piStartDate: string;
  piEndDate: string;
}

export interface EpicAdvancedAnalytics {
  raid?: string;
  wsjf?: string;
  piScope?: string;
  progress?: string;
} 